# Unique-Request-Angular-http-decorator
Prevent Multiple request for the same url in angular. 
Ex: when multiple service ask for the same service
Just Drop it in your project and it should do the trick.
